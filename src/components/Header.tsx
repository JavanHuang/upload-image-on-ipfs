import React, { FC } from 'react';

type Props = {}

const Header: FC<Props> = ({ }) => {
  return (
    <header className="App-header">
    <div>IPFS Test Image Uploader
    </div>
  </header>
  )
}

export default Header